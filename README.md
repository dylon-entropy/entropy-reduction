# 熵减 (Entropy Reduction)

**A spec-driven AI programming workflow that works with any AI coding tool.**

熵减定义了一种**人与 AI 协作编程的方式**：分工明确，人只做 AI 做不了的事（业务判断、设计审核），AI 负责所有可自动化的环节（生成文档、写代码、跑测试、维护一致性）。

它不是一个 IDE 或插件——它是一组 skill 文件，可以安装到 Claude Code、OpenClaw、Cursor、Codex、Gemini CLI 等任何支持 skill/prompt 机制的 AI 编程工具中。

---

## 为什么需要它

AI 编程工具很强，但直接让 AI 写代码会遇到这些问题：

- **上下文漂移**：AI 忘了前面的设计决策，实现偏离需求
- **自由发挥**：没有约束时 AI 会编造 URL、猜测字段名、跳过错误处理
- **无法积累经验**：同样的错误在不同会话里反复出现
- **改动失控**：改一个接口，相关的文档、缓存、测试全部漏改
- **熵增**：AI 每次生成都是“重新开始”，项目越改越乱，最终无法维护

熵减用**结构约束**解决这些问题，而不是依赖 AI 的“自觉”。

---

## 设计哲学

- **文档是契约，不是注释**——先有设计文档，再写代码。Spec 是结构化的，有接口定义、JSON 示例、参数校验表格，人审核时不需要去读 AI 的对话理解它做了什么
- **人做判断，机器做执行**——泾渭分明。你只做三件事：提供需求、审核设计、确认结果。其余全部由 AI 完成
- **对抗熵增，而非假装它不存在**——用结构代替信任，用文件代替记忆，用分工代替全能
- **针对当前，不押注未来**——基于今天 AI 的真实能力边界设计，不假设 AI 明天就能自主完成一切。随着 AI 进化，工作流同步升级，人审核的环节会逐步减少，但“文档驱动”和“结构约束”的核心不会变

---

## 对话即工作流

你不需要记住指令或操心“该走什么流程”。直接用自然语言对话：

- 说“帮我加一个用户标签功能” → 常驻规则自动识别为新需求，走 spec 流程
- 说“这个接口要加一个字段” → 自动触发改动影响判断，先检查 spec 再改代码
- 说“跑一下测试” → 自动进入 verify 阶段

当然，你也可以用精确指令（`/er-spec`、`/er-dev`、`/er-test`）直接触发。对于能力较弱的模型，推荐使用指令以获得更可靠的触发效果。

> **注意**：自动触发的可靠性取决于模型能力。Opus / Sonnet-4 级别的模型可以很好地识别意图，较弱的模型可能需要手动使用指令。

---

## 降低审核成本，而非增加

人审核是必须的——这是质量的唯一保证。但熵减让审核变快了：

- Spec 是结构化的，人工确认区把待决策项集中在一处，不用翻来翻去找
- 代码实现后自动对照 spec 自检，审核时只需要关注重点差异
- AI 犯的错会被记录到 learning-log，同类错误出现 2 次自动触发固化——审核问题越来越少

---

## 核心价值：对抗 AI 编程的熵增

很多人担心：用 AI 写代码，项目会很快变得无法维护——命名混乱、重复逻辑、不一致的抽象、没人能完全理解的“幽灵代码”。这不是危言耸听，而是 AI 的天然倾向：每次生成都是“重新开始”，没有全局约束，熵自然增加。

熵减通过 **Spec 驱动 + 学习机制** 对抗熵增：

- **Spec 是全局契约**：所有代码必须遵守同一套数据格式、接口定义、业务规则，AI 没有机会“自由发挥”
- **学习日志固化**：每次错误被记录，同类错误出现 2 次后自动提议固化到规则中——项目越用越“聪明”，而不是越用越乱
- **一致性自动同步**：改一处，所有引用自动更新，避免文档与代码的熵增

结果是：AI 生成的代码**可预测、可维护、可传承**，而不是一次性玩具。

---

## 工作流程（一图看懂）

```
需求描述
  ↓ /er-spec（生成技术文档）
全局约束 + 自包含 Spec 文件
  ↓ 人审核设计
  ↓ /er-dev（按 Spec 实现代码）
代码实现
  ↓ /er-test（验证测试）
测试通过 → 自动 Git 提交
```

> 每个阶段结束后，AI **不会自动跳转**，必须等你确认。你永远掌控节奏。

---

## 最小可用子集（5 分钟上手）

你**不需要**一开始就用全部功能。从两个命令开始，就能获得核心价值：

1. `/er-spec` —— 让 AI 根据你的需求生成设计文档（你只需确认关键点）
2. `/er-dev` —— 让 AI 严格按照文档写代码

仅此两步，AI 就不再“猜测”你的意图，代码第一次就做对。

当你需要更多时，再逐步引入测试、审核、交接、效能分析。

---

## 完整流程的价值（每个阶段都有回报）

| 阶段 | 看起来 | 实际价值 |
|------|--------|----------|
| **spec** | 写文档 | AI 不再猜测，代码第一次就符合预期 |
| **人工审核** | 打断流程 | 在设计阶段发现缺陷，避免编码后返工 |
| **dev** | 写代码 | AI 按 spec 填空，不跑偏、不发明 |
| **test** | 写测试 | AI 自动生成并运行，你无需动手 |
| **review** | 代码审查 | 两个维度：①对照 spec 检查功能偏差；②纯代码质量（性能、错误处理、并发等） |
| **handoff** | 交接文件 | AI **自动检测**上下文退化（质量下降、遗忘约定），主动提醒你交接。你也可以手动 `/er-handoff` 保存进度 |
| **metrics** | 效能分析 | 可选，想看效率数据时运行 `/er-metrics` |
| **learning** | 学习固化 | 自动记录错误模式，积累证据后询问你是否固化到规则中（后台运行，不打扰） |

如果你觉得某个阶段当前项目不需要，**直接跳过**——熵减不会强制你。它只是一套经过验证的选项，不是牢笼。

---



## 快速开始

> 最推荐Claude Code系列sonnet4.5及以上模型，实践效果最好

### Claude Code（推荐）

```bash
# 方式1：Plugin marketplace（一键安装）
/plugin marketplace add dylon-entropy/entropy-reduction
/plugin install entropy-reduction

# 务必不要忘记这一步，直接影响工作流能否被正确触发的概率，{1.0.0}可根据实际版本进行替换
cat ~/.claude/plugins/cache/entropy-reduction/entropy-reduction/1.0.0/platforms/claude-rules.md >> ~/.claude/CLAUDE.md


# 方式2：手动安装
git clone https://github.com/dylon-entropy/entropy-reduction.git
cd entropy-reduction && bash install.sh

```

### 验证
在 AI 对话中输入：

```
/er-init
```
AI 应该能识别并加载 er-init skill，输出初始化结果。如果没有反应，检查：
1. skill 文件是否在正确目录
2. 常驻规则是否已追加到配置文件
3. 配置文件是否被当前 AI 工具加载

### 升级

```bash
# Plugin 安装方式
/plugin update entropy-reduction

# Git clone 安装方式
cd ~/.claude/skills/entropy-reduction && git pull
```

Skills 更新后立刻生效。如果常驻规则有变更（如新增触发词），需要手动更新配置文件中的对应内容——CHANGELOG 会注明哪些版本涉及常驻规则变更。

---

## 技能清单

| 技能 | 指令 | 触发词 | 说明 |
|------|------|--------|------|
| er-spec | `/er-spec` | "生成技术文档""写 spec" | 从需求生成全局约束 + 自包含 spec |
| er-dev | `/er-dev` | "按设计实现""写代码" | 严格按 spec 实现代码 |
| er-test | `/er-test` | "验证""跑测试" | 自动生成测试并运行 |
| er-review | `/er-review` | "review""审核代码" | 双维度审核：①对照 spec 检查功能偏差；②纯代码质量（性能、错误处理、并发等） |
| er-handoff | `/er-handoff` | "交接""保存进度" | 生成交接文件。**AI 也会自动检测上下文退化并主动提醒你交接** |
| er-init | `/er-init` | "熵减初始化" | 从交接文件恢复工作状态 |
| er-git | 自动调用 | — | 分支管理 + pre-commit hook |
| er-sync | 自动调用 | — | 文档一致性同步 |
| er-metrics | `/er-metrics` | — | 效能分析（可选，不运行不影响工作流） |
| er-learning | 自动调用 | — | 学习固化（自动记录，偶尔询问是否固化） |

> **注**：`er-metrics` 和 `er-learning` 是后台机制，你不需要主动使用它们。`er-metrics` 仅在你想查看效率数据时运行；`er-learning` 自动记录错误模式，积累足够证据后会询问你是否固化到规则中。

---

## 与其他工具的关系

熵减不替代任何 AI 编程工具，它是在这些工具之上附加的一套**方法论 + Skill 集**。你可以把它理解为：

- 一套**人机协作的流程规范**（文档驱动、阶段确认、学习固化）
- 一组**可安装的 Skill 文件**（适用于 Claude Code、OpenClaw、Cursor、Codex、Gemini CLI 等）

如果你熟悉社区中其他 AI 编程辅助方案，熵减在设计上关注以下几个维度：

- **Spec 驱动**：设计文档是代码实现的唯一依据，而非可选的注释
- **学习机制**：通过 learning-log 记录错误模式，同类问题出现 2 次后自动提议固化到规则中
- **人审核控制**：阶段之间强制等待人工确认，避免 AI 自主跳转
- **跨平台支持**：不绑定特定 IDE 或 AI 工具，可安装在多种环境中
- **中文优先**：文档、注释、提示词均以中文为主

这些只是不同的设计取舍，并非优劣判断。你可以根据自己的工作习惯和团队需求选择最合适的方案。

---

## 文档

- [设计哲学](docs/philosophy.md)
- [FAQ](docs/faq.md)
- [快速上手示例](examples/quickstart.md)

## 项目结构

```
entropy-reduction/
├── README.md
├── LICENSE
├── install.sh              # 手动安装脚本
├── .claude-plugin/         # Claude Code plugin 适配
├── .cursor-plugin/         # Cursor plugin 适配
├── .codex/                 # Codex 安装指引
├── skills/                 # 核心 skill 文件（10 个）
├── platforms/              # 各平台常驻规则
├── docs/                   # 文档
└── examples/               # 使用示例
```

## License

MIT
